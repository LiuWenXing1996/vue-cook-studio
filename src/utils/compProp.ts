import {EditorConfigInput} from "../components/EditorInput.vue"

export interface ComponentProp {
    name: string,
    value: string,
}

export interface ComponentProp {
    name: string,
    value: EditorConfigInput,
}